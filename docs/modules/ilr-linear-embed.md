# ILR Linear Embedding

This command is now documented as [Linear embedding](linear-embed.md), which
supports both raw factor-proportion and ILR projection spaces. The command
`punkst ilr-linear-embed` remains available as an alias for
`punkst linear-embed`.
